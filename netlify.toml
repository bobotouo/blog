# Nuxt 3 + Nitro：构建产物在 .output，详见 docs/DEPLOY-NETLIFY.md
[build]
  command = "npm run build && npm run build:netlify-dist"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NITRO_PRESET = "netlify"
