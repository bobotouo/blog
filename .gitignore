.DS_Store
.env
.env.*
.nuxt
.output
.vercel
.netlify
dist
node_modules
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# 注意：勿将 public 下由 generate-blog-list 生成的 JSON 再列入 ignore。
# Vite 复制 public 到产物时会跳过 .gitignore 中的路径，导致静态站预渲染拿不到数据（500/404）。
